# The Monitoring Tool SetUp Repo

## This repo consists all docker files to create a monitoring setup using `prometheus, grafana, promtail, node-exporter, loki` . with these repo you can setup monitoring on your pc or desired pc
## Requirements - docker should be installed and configured. OS should be ubuntu.

# Mukesh Kumar Sharma
